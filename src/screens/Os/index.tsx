import { Text, VStack } from "native-base";

export default function Os() {
    return(
        <VStack flex={1} justifyContent={"center"} alignItems={"center"}>
            <Text color="gray.100">OS</Text>
        </VStack>
    )
}

